import { PaginateViaUrl } from './paginate-via-url';

describe('PaginateViaUrl', () => {
  it('should create an instance', () => {
    const directive = new PaginateViaUrl();
    expect(directive).toBeTruthy();
  });
});
