import { SortViaUrl } from './sort-via-url';

describe('SortViaUrl', () => {
  it('should create an instance', () => {
    const directive = new SortViaUrl();
    expect(directive).toBeTruthy();
  });
});
